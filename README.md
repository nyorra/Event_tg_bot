# HR_COFE_BOT

Проект представляет собой сервис на aiogram, предоставляющий интерфейс для коллаборации с сетью кофеен.
Сервис позволяет пользователям взаимодействовать в рамках временного 8-ми недельного ивента. Проект контролируеться
группой администраторов в Telegram, с использованием Admin panel.

---

## 📦 Технологии

- Python 3.13
- PostgreSQL
- Aiogram
- Docker
- Alembic

---

## 🧰 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/nyorra/Coffee_tg_bot.git
cd Coffee_tg_bot
```

---

### 2. Установка зависимостей


```bash
pip install -r requirements.txt
```

---

### 3. Настройка переменных окружения

Создай `.env` файл из шаблона:

```bash
cp .env.example .env
```

Открой `.env` и заполни все необходимые переменные.

---

### 4. Использование makefile - линтеры для поддержки программного кода.

#### 🔄 Применение миграций:

```bash
uv run ruff check --fix bot
```

```bash
uv run ruff format bot
```